export enum Allow_Comment_Enum {
  allow = "allow",
  deny = "deny",
}

export enum Availability_Enum {
  public = "public",
  only_me = "only_me",
  friends = "friends",
}

export enum Like_DisLike_Enum {
  like = "like",
  disLike = "disLike",
}


// post or comment
export enum On_Model_Enum {
  Post = "Post",
  Comment = "Comment",
}
