export const multer_enum = {
  image: ["image/png", "image/jpeg", "image/jpg", "image/webp"],
  video: ["video/mp4"],
  pdf: ["application/pdf"],
};

export enum Store_Enum {
  disk = "disk",
  memory = "memory",
}
